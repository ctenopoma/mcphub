# ===========================
# DinD MCP Hub Configuration
# ===========================

# Host bind address (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
BIND_HOST=0.0.0.0

# Traefik reverse proxy port (routes to child container APIs and IDEs)
TRAEFIK_PORT=8085

# Rust Management UI port
UI_PORT=8081

# Python MCP SSE server port
MCP_PORT=8000

# Manager dashboard password
MANAGER_PASSWORD=mcp-hub-password
